# v0.6 Local LLM Companion VM QA - 2026-06-21

## Scope

This pass covers the v0.6 local LLM companion slice:

- desktop settings for local runtime URL, model id, and timeout;
- packaged companion runtime status check;
- packaged companion runtime doctor check;
- loopback-only Rust validation from the installed Tauri app;
- selected-text LLM suggestion UI and manual-apply path through component tests;
- installer smoke on Hyper-V VM `LisanStudio-QA`.

## Local Gates

All commands were run from `C:\CodexProjects\Alfaraheedi` unless noted.

| Gate | Result | Evidence |
| --- | --- | --- |
| `cargo fmt --all --check` | Pass | Exit 0 after rustfmt. |
| `cargo test -p alfaraheedi-desktop` | Pass | 11 passed, 0 failed. |
| `cargo test -p write-llm` | Pass | 10 passed, 0 failed. |
| `cargo test -p write-service` | Pass | 5 passed, 0 failed. |
| `npm test -- companion.test.ts companionLlmUi.test.tsx` | Pass | 2 files, 8 tests passed. |
| `npm run typecheck` | Pass | `tsc -b --noEmit` exit 0. |
| `npm run lint` | Pass | ESLint exit 0. |
| `npm test` | Pass | 12 files, 27 tests passed. |
| `cargo clippy -p alfaraheedi-desktop -p write-llm -p write-service -- -D warnings` | Pass | Exit 0. |
| `npm run build` | Pass | Vite build exit 0; bundle-size warning only. |
| `cargo test --workspace` | Pass | Workspace tests passed. |
| `cargo clippy --workspace -- -D warnings` | Pass | Exit 0. |
| `cargo run -p write-eval` | Pass | 25 cases, precision 1.0, recall 1.0, no failures. |
| `cargo deny check licenses bans sources` | Pass with warnings | licenses/bans/sources ok; duplicate-version warnings only. |
| `npm run test:e2e` | Pass | 12 Playwright tests passed across desktop/mobile Chromium, including automated accessibility checks. |
| `npm run desktop:build` from `frontend` | Pass | Built NSIS installer and copied `Alfaraheedi-0.5.0-windows-x64-setup.exe`. |

## VM Install Smoke

VM: `LisanStudio-QA`

Installer copied to:

```text
C:\QA\Alfaraheedi\Alfaraheedi-0.5.0-windows-x64-setup.exe
```

Host and VM installer SHA-256:

```text
F0E5EC3F46BD4D29435691C968646FAF9EAD34574B67EB7D96D7BD36E89C3AF5
```

Silent install:

```powershell
Start-Process -FilePath C:\QA\Alfaraheedi\Alfaraheedi-0.5.0-windows-x64-setup.exe -ArgumentList '/S' -Wait -PassThru
```

Result:

- Exit code: `0`.
- Installed exe: `C:\Users\codexqa\AppData\Local\Alfaraheedi\alfaraheedi.exe`.
- File version: `0.5.0`.
- Installed exe timestamp: `2026-06-21 19:42:38`.
- Installed exe SHA-256: `1F190A5A99671BAA86CE98A89178BCF24BC8F880FCCF4E09DF938F15097320C2`.

## VM Desktop Smoke

A scheduled task was used to run inside the guest desktop session because PowerShell Direct itself runs in session `0`.

Result:

- Scheduled smoke script ran in session `1`.
- Installed app process started.
- Main window appeared.
- Window title: `Alfaraheedi`.
- Process remained alive and responding.

UI Automation limitation:

- UI Automation exposed the WebView container and window chrome, but not the React DOM text.
- Screenshot capture failed in the disconnected desktop session with `The handle is invalid`.

## VM Packaged DOM/IPC Smoke

To inspect the packaged WebView in the disconnected VM, the app was launched with a temporary WebView2 debug flag:

```text
WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS=--remote-debugging-port=9222 --remote-allow-origins=*
```

This was used only inside the VM QA session.

CDP verified the installed packaged DOM at `http://tauri.localhost/`:

- LLM settings section rendered.
- Check runtime button rendered.
- Run doctor button rendered.
- The VM still had an invalid persisted runtime URL from an earlier policy smoke, so clicking Check runtime showed the Rust-side policy rejection:

```text
ALFARAHEEDI_LLM_BASE_URL must point at a local loopback runtime, for example http://127.0.0.1:8000
```

- Clicking Run doctor opened the runtime doctor panel and showed:

```text
local LLM doctor found blocking configuration issues
```

- The doctor check list included `runtime_config` with outcome `fail` and the same `ALFARAHEEDI_LLM_BASE_URL` loopback hint.

This verifies the packaged frontend can call the Tauri runtime status and doctor commands, and that the installed Rust host rejects non-loopback LLM runtimes without attempting live model traffic.

## Not Verified In This VM Pass

- Real hotkey/cross-app clipboard capture and replacement in the VM. The guest desktop session was disconnected; this was already a known limitation for input-injection QA in this VM lane.
- Real local model quality or latency. No GGUF model/runtime was installed in the VM.
- End-to-end selected-text LLM suggestion from a real model. The UI/manual-apply path is covered by component tests; the installed-app runtime status and loopback policy path are covered by VM CDP.

## Findings

No blockers found in this pass.

Residual risks:

- The installer still reports version `0.5.0` because this is a development branch and release version bump is not part of this slice.
- The frontend bundle remains slightly above Vite's default 500 kB warning threshold; this predates this pass and did not fail build or tests.
